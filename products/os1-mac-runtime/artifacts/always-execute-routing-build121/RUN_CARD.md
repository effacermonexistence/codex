# Always-execute routing, bounded read-only shell, UI integration — build121
Goal: while any backend exists, no request ends in a preflight refusal; read-only check/inspection requests run on Claude's lane with a bounded shell when Codex is unavailable; the UI fixes that OS-1-driven Claude sessions pushed as branches (composer drag-and-drop, sidebar row tap area, provider-rail highlight) reach the installed app.
Source: installed build120; task-events `c49fd192-…` (13:20:26Z "…그거 좀 제대로 고치지 코덱스랑 일치시키면 돼" → "이 작업에 필요한 Codex 실행 환경이 없습니다 (… 한도 … spark …)"), `da245d43-…` (rail highlight complaint), remote branches `os1/composer-drag-drop-files-20260914` (f62e480), `fix/sidebar-row-tap-and-divider` (80331c3), commits 6c57ed4/00fab42 on the build branch; owner's rule "코덱스가 죽어 있어도 라우팅해서 무조건 수행".
Allowed: provider-preference fallback, read-only lane Bash allow rules and sandbox `excludedCommands` for inspection commands, dontAsk denials treated as the bound in that lane, scope markers for informal fix wording, merging the OS-1-driven UI branches, self-test regressions, release version and local verified installer.
Forbidden: giving the read-only lane write commands or a generic shell (`cat`/`ls`/`curl` excluded), changing Codex/Claude user configuration, replaying write-profile work, held queue replay, external deployment.
Failure: (1) `executableProviderPreference` refused whenever the constrained/pinned backend was unavailable even though the other backend could execute; (2) Claude's read-only lane had no shell at all, so status/verification objectives could only be refused; when Bash was added, the process sandbox auto-denied network/keychain under dontAsk and one denial became a terminal `policyDenied`; (3) "고치지/일치시키면 돼" resolved as read-only; (4) UI fixes made through OS-1 were committed and pushed but never built or installed.
Expected: unavailable pinned/constrained backend → the other backend with a routing notice; only "no backend at all" stops; the read-only lane runs `git`/`gh`/`railway`/`wrangler … get` inspection commands unsandboxed via `--settings` `excludedCommands`, everything else denied without prompt and reported as a bound, not a verdict; informal fix wording is a write scope; the installed app carries the sidebar and rail-highlight changes, and files dropped anywhere in the window (or picked with +) become Codex-style attachment chips — image preview for images, icon+name chip for other files, removable — that are sent as the existing quoted "참조 파일 경로" block (attachments alone are sendable).
Validation: self-test (48 completion checks + permission orchestration incl. bounded-lane fixture), debug replay "GitHub effacermonexistence/codex 최신 상태 확인해 …" from HOME (build120: two dead ends; build121 debug: `claude/sonnet/medium` adopted 41 s with real `gh` output), full release build, verified local install with queue preservation, replay on the installed build.
Stop: adopted local install + replay completes and the UI branches are integrated, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via the private installer receipts under `~/.os1/recovery/`; preserve the latest session file.
Residual risk (documented): prefix allow rules cannot stop flag-driven writes such as `git diff --output=…`; the lane is a guard against accidental writes, not a security boundary — the workspace_write lane already has full Bash.

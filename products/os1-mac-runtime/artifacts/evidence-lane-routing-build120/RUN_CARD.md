# Evidence-lane routing, quoted-output classification and contract retry — build120
Goal: a request that OS-1 satisfies with its own verified R2 evidence ("QM이랑 GR자료 R2에서 다 가져와 설명해 … 스키마 다 짜") must not be refused by the Codex-only shell preflight while no Codex model is available; pasted OS-1 output must not be classified as the user's question; an answer that fails a local contract must get one corrected retry instead of ending the run.
Source: installed build118, task-events `de8c1b88-…` (12:37:27Z → "이 작업에 필요한 Codex 실행 환경이 없습니다") and `49479d78-…` (pasted transcript + "고쳐" → "복원 가능 여부를 묻는 질문에는 변경 권한을 사용하지 않습니다"); installed build119 replay `d25033ae-…` (opus answer rejected as `capability_unavailable` because it said it did not execute anything, then "서버의 완료 판정과 실제 실행 증거가 일치하지 않아"); `executableProviderPreference`, `asksRecoveryReadiness`, `providerOutputDeclaresCapabilityFailure`, the local-rejection guard in `main.swift`.
Allowed: provider-preference preflight conditions, exclusion reasons in the advertised catalog source, a quoted-output line filter for intent classifiers, the evidence-supplied exemption in the capability-refusal heuristic, one bounded read-only retry carrying the rejection diagnostic, self-test regressions, release version and local verified installer.
Forbidden: granting the read-only Claude lane a shell, changing Codex/Claude configuration, replaying write-profile work, held queue replay, external deployment, unrelated UI or runtime rewrite.
Failure: (1) any prompt mentioning R2/GitHub with fetch/check wording was forced to Codex even when OS-1 itself retrieves the evidence or the ticket carries a write profile; with the Codex catalog emptied by the exhausted quota and the context budget the preflight refused before any model. (2) `asksRecoveryReadiness` matched "복구 기준점(Gold 포인터)" from OS-1's own quoted answer plus the user's "가능하냐". (3) An evidence-backed read-only answer that honestly said it did not run anything matched the capability-refusal markers and was rejected; the route service had already accepted the artifact, so the run ended with no retry.
Expected: the Codex-only constraint applies only to shell-bound objectives (no supplied evidence and not a write profile) and names the exclusion reasons when it still refuses; intent classifiers evaluate the user's own lines after `OS1SelfOutput.stripQuoted`; with supplied evidence and no repair request the capability-refusal heuristic does not fire; a locally rejected read-only candidate is retried once with `BackendContinuation.diagnostic` in the handoff, within the attempt limit, never for write profiles.
Validation: self-test cases (evidence-backed and write-scope wording route to Claude without Codex; read-only shell wording still stops; quoted output stripped; evidence-backed no-execution note accepted; repair request keeps the refusal rule; continuation carries the diagnostic), debug replays of "QM이랑 GR자료 R2에서 다 가져와 설명만 해" from HOME (build119 debug adopted; build119 installed rejected → motivated (3); build120 debug adopted first try), full release build, verified local install with queue preservation, replay on the installed build.
Stop: adopted local install + replay completes, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via the private installer receipts under `~/.os1/recovery/`; preserve the latest session file.
Not changed: a read-only shell objective (e.g. "GitHub 최신 상태 확인해") with no Codex model still stops before any model call; the message now says why (quota reset time, context budget). Build119 was installed and replaced by build120 within minutes; both receipts are kept.

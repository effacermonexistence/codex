# Owner-voice guard, clean-readback resume, live settings — build133
Goal: only the owner speaks in the owner's chat. And a reconciliation that verifies "nothing was applied" must release the hold and finish the owner's original request by itself, instead of diagnosing and stopping.
Source: owner: "코덱스가 왜 사라져버렸어 고쳐" + "나는 쓰지도 않았는데 왜 자기가 채팅창에 채팅을 치는데? 그거 고쳐. 얘가 고치려고 하던거 못고치거든 … 자기들 자신을 픽스할 수 있게 고치라니까". Evidence: settings.json `showCodex:false` (my own toggle test on Sep 14 19:17, never restored — the missing rail was my doing); a `role: "user"` message with `nativeIngestedID: claude:4641bf2c…` whose text is `BackendRecovery.readbackPrompt` verbatim — OS-1's own reconciliation prompt re-ingested from the bound native session as the owner's message; the read-only reconciliation correctly diagnosed the cause but ended with "원래 작업은 아직 미완료"; this very request's fleet job ran as a connection_check because the pasted transcript carried "연결"+"Claude" across different lines.
Allowed: `NativeIngestion.isOS1ControlPrompt` (start-anchored on OS-1's two dispatch openers) filtering user-role records; `OS1_EFFECTS` verdict contract in the readback prompt + parser (`EffectsVerdict`, case `nothingApplied` to dodge the Optional.none pitfall); one-shot auto-resume of the preserved objective when the readback verdict is exactly `none` (releases the uncertain-effect hold, announced, cancellation-checked, `readbackResumed` budget); maintenance-tick reload of settings.json; per-line connection-intent matching; settings restore `showCodex:true`; fixtures; build133 staged and self-installed.
Forbidden: resuming on `applied`/`partial`/`unknown` or on prose without the verdict line; a second auto-resume for the same failure; deleting the already-recorded phantom message from the live store (history stays; the class is fixed); weakening the rule that an owner-led message quoting internal text stays the owner's.
Failure modes fixed: (1) OS-1's readback prompt replayed into the conversation as the owner's words via native ingestion; (2) verified-clean reconciliations still left the objective held, so "고쳐" ended as a diagnosis; (3) a settings change from outside the app (CLI/file) was invisible until relaunch — my restored `showCodex:true` would not have brought the rail back; (4) connection verbs and service names in a pasted transcript, lines apart, hijacked requests into connection checks.
Validation: backend-recovery fixtures 114 (verdict grammar: parse/case/space, last-line-wins, explained-verdict void, prose void; readback prompt carries the contract; control-prompt recognition; ingestion drops the phantom, keeps the owner's turn), app native-provenance suite updated to the new contract (verbatim control text dropped, owner-led quote kept), connection classifier self-test with the multi-line paste negative, full CLI/fleet/app suites, stage + self-install as build133.
Stop: build133 self-installed, CODEX rail visible again, receipts posted.
Rollback: previous binaries under `~/.os1/recovery/self-update-build132-*`; settings.json edit is a one-field revert.

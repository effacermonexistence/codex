# OS-1 installs OS-1, and keeps working without a window — build125→130
Goal: "OS1 고쳐"가 OS-1 안에서 100% 끝나야 한다. 진단 → 자기 소스 수정 → 빌드 → 테스트 → 스테이징 → **OS-1이 스스로 설치** → 재시작 → 그 대화에 영수증. 그리고 "자기 자신한테도 RCC 통제 로직 적용하는 거 아니야?"에 코드로 답한다.
Source: owner "야 100% 되게 해라니까 왜 부분적으로만 되는데 고쳐", "자기 자신한테도 그 RCC 통제 로직 적용하는 거 아니야? 맞으면 맞다 하고 아니면 그렇게 하게 고쳐". OS-1 자신의 read-only 답변이 "부분적으로만 가능, 재설치는 사람이" 라고 끝난 것이 출발점.
Allowed: SelfUpdate 의도/결과 보관, `os1 self-update stage|apply|status`, 백엔드에 주는 자기수리 계약, 앱의 설치 코디네이터와 영수증 게시, OS-1 소스 쓰기 리스, 설치기의 의미 비교, 유지보수 루프를 스토어 소유로 이동, 픽스처/셀프테스트, 릴리스·검증 설치.
Forbidden: 설치기 우회(서명·지정요건·셀프테스트·유휴/대기열 검사 유지), 실행 중 작업이 있을 때 설치, 대기열 재생·삭제, 자격증명 취급, 영수증 없이 "설치됨" 주장.
Failure (실측):
1. `install-local-verified.mjs`가 세션 저장소를 **바이트**로 비교 → 키 순서만 다른 재저장에도 실패 (build125). 키 정규화 비교 결과 84세션/554메시지 완전 동일.
2. 릴리스 스크립트가 `OS1_VERSION` 기본값(0.9.56)과 번들 버전 불일치로 0.9.57부터 **조용히 패키징 중단**. 스테이지 앱만 만들어져 로컬 설치는 됐지만 pkg·매니페스트는 없었음.
3. 복구·자체설치·영수증이 **네이티브 사이드바 폴링 안에** 있어, 백엔드 읽기가 한 사이클 멈추면 전부 굶음 (build127이 build128 의도를 15분 이상 방치).
4. 영수증을 게시하지 못해도 `markReported`가 실행돼 **영수증이 소실**(build127: 메시지도 태스크 이벤트도 없이 reported=true).
5. 결정타: 설치기가 `open -g`로 앱을 백그라운드 재실행 → **창 0개** → RootView의 `.task`가 시작되지 않음 → 사이드바 동기화·복구·자체설치·영수증까지 전부 정지. 실측: `windows = 0`, 앱은 살아있는데 아무 주기 작업도 하지 않음.
Expected: 쓰기 작업이 OS-1 소스를 고치면 `self-update stage`가 서명 릴리스를 만들고 3종 셀프테스트를 통과시킨 뒤 의도를 남긴다. 실행 중인 OS-1은 **창이 없어도** 3초 틱에서 이를 발견해 설치기를 분리 실행하고, 설치기가 이진을 교체·검증·재시작하며, 재시작한 앱이 그 대화에 영수증을 남긴다. 영수증은 디스크에 남은 것을 확인한 뒤에만 소비한다. 같은 체크아웃에 두 쓰기 작업이 겹치면 리스에서 대기하다 보존한다.
Validation: SelfUpdate 픽스처 19, BackendHealth 22, Localization 10, CLI 56, Fleet 24, 앱 self-update 리포트 12(창 없는 루프 소유·중복 방지·내구성 포함) + 기존 전 스위트; build126·127 자동 셀프설치 실측, build129·130 설치 후 **창 0개 상태에서 영수증 자동 게시** 실측.
Stop: 창 없는 앱이 스스로 설치하고 영수증까지 남기는 것을 확인하면 종료.
Rollback: `~/.os1/recovery/self-update-build*/`의 이전 앱·CLI, 설정은 settings.json 삭제로 초기화.

# Settings pane, English default + multilingual, Codex toggle, RCC on itself — build126
Goal: OS-1 gets a Codex-style Settings pane (Cmd+,): interface language (default English, Korean, follow-system), response language (auto = answer in the language the user typed; or pinned), and a "Use the Codex backend" toggle that removes Codex from the rail and from routing. And the owner's challenge "일 처리할 때 자기 자신한테도 RCC 통제 로직 적용하는 거 아니야?" is answered in code: OS-1's own source tree gets the same write discipline (one writer at a time) and the installer's store-integrity check stops mistaking a legitimate re-encode for tampering.
Source: owner (via OS-1): "default 언어 영어로 설정하고 다국어로 해 … 세팅에 가서 사람들이 클로드 코드나 코덱스처럼 설정할 수 있으면 … 코덱스는 뺏기면 돼 … 배포하면 키보드는 영어일 거고 출력은 당연히 다른 거고"; owner (direct): "그런 멍청한 드리프트가 난다고? … 자기 자신한테도 그 RCC 통제 로직 적용하는 거 아니야? 맞으면 맞다 하고 아니면 그렇게 하게 고쳐". Observed drifts: build125 install failed only at `assert(bytes equal)` because the user relaunched the app mid-install and it re-saved sessions.json with reordered keys (key-normalized diff: 84 sessions/554 messages identical); two writers (this session + an OS-1-driven Claude run) edited the same checkout concurrently with no lease.
Allowed: `OS1Settings` (settings.json, English/auto/Codex-on defaults), `os1Tr` + `OS1Localization` (env override for fixtures), Settings scene + view, rail filter, runtime routing guard for disabled Codex (`BackendHealth.State.disabled`), output-language directive as its own prompt section before the terminal user request, `acquireOS1SourceWriteLease` for workspace_write runs in the OS-1 checkout and for `self-update stage`, installer deep-equal store check, localized runtime surfaces (RuntimeActivity labels, BackendBlocker messages, ConnectionFailure, app menus/home/statuses), fixtures, version 0.9.60 build126, live self-install through the running build125 app.
Forbidden: weakening the installer's store protection (deep-equal still fails on any content change; per-message preservation checks stay), auto-translating persisted conversation content, hiding Codex from fleet heartbeats when it is merely settings-disabled on another machine's behalf, touching credentials.
Failure modes fixed: (1) byte-compare false positive → semantic compare; (2) no self-write lease → two agents interleaving in one checkout; (3) no user-facing settings at all — language and backends were hardcoded; (4) Korean-only chrome for an English-keyboard audience.
Expected: Cmd+, opens Settings; interface default English (owner can pick 한국어); "auto" answers Korean to Korean input with an English keyboard; response language pinned adds one OUTPUT LANGUAGE section; Codex off → rail shows OS-1/CLAUDE only, runtime routes nothing to Codex, health reads "설정에서 꺼짐" with no repair steps; concurrent OS-1 write tasks wait on the lease (≤180 s) then preserve.
Validation: Localization fixture 10 checks; CLI self-test 56 (directive placement/terminality, disabled-Codex health); fleet 24; app self-tests incl. settings-driven rail; full suite green; staged and self-installed as build126 by the running build125 app (the end-to-end "OS-1 repairs OS-1" proof), receipt in the conversation and `~/.os1/recovery/self-update-build126-*`.
Residual: deep runtime diagnostics (BackendHealth diagnosis lines, self-repair notes, governance/receipt strings) remain Korean-first this build — listed for the next localization pass; interface language covers menus, home, key statuses, Settings, activity labels and blocker messages.
Stop: build126 self-installed with receipt, or a genuine unsafe active-state blocker.
Rollback: previous binaries under `~/.os1/recovery/`; settings.json can simply be deleted to restore defaults.

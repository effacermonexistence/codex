# Backend context-overflow and exhausted-quota recovery — build117
Goal: an OS1 request must not die when the only advertised Codex model cannot hold the account's base instructions; classify the overflow, route within Codex first, pre-exclude models that cannot fit, and let the read-only Claude lane answer feasibility questions.
Source: installed build114, active source tree, run journal `8064E376-27C4-4982-8C0B-D34EC05878D9`, Codex rollouts `01a09f72-07f4/2230/3b1b` (`codex_error_info: context_window_exceeded`), app-server `account/rateLimits/read` (general `codex` bucket 100% used, resets 2026-09-19T20:51:15Z), user report "실행 결과를 확인하지 못했습니다".
Allowed: backend failure classification, recovery routing within the existing signed-ticket protocol, advertised-catalog capacity filtering from public account metadata, one heuristic exemption for feasibility questions, self-test and fixture regressions, release version and local verified installer.
Forbidden: changing the user's Codex configuration or base instructions, authentication/session migration, held queue replay, paid model calls beyond the proof runs, external deployment, unrelated UI or runtime rewrite.
Failure (three layers): (1) `gpt-5.3-codex-spark` (121,600-token window) receives ≈199K tokens of configured base instructions and rejects the turn unread; (2) OS1 recorded that as `quality_failure`, the route service escalated effort on the same model three times, then `unclassified`; (3) the account's weekly Codex quota is exhausted, so `CodexQuota` correctly excluded every larger model and only spark was advertised, while the read-only Claude lane was blocked pre-dispatch because "테스트 해보자" matched the shell-capability rule.
Expected: `codexErrorInfo == context_window_exceeded` → `context_overflow` blocker, `capability_failure` outcome; an overflowing slug is removed and another Codex model is requested first, otherwise the alternate backend; models whose cached `context_window` is below the configured `model_instructions_file` size plus margin are never advertised (`CodexContextBudget`); quota and capacity exclusions are announced in the run activity; a feasibility question ("…가능하냐?") does not require the shell lane; an untouched workspace never becomes `effects_uncertain`.
Validation: BackendRecovery fixtures (`OS1ContextTests`), runtime self-test cases (classification, outcome, alternate, classifiedBlocker, context budget, quota reset description, feasibility heuristic), full release build, verified local install with queue preservation, and the original failing request replayed on the installed build until it completes.
Stop: adopted local install + the replayed request completes, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via the private installer receipts under `~/.os1/recovery/context-overflow-build11[567]-*`; preserve the latest session file.
History: build115 (classification + alternate) and build116 (Codex re-route) were installed and replayed first; instrumentation showed the advertised catalog was `[gpt-5.3-codex-spark]` because of the exhausted quota, which led to the build117 catalog and heuristic changes. Debug replay on build117 completed in one attempt (`claude/sonnet/medium → adopted`).

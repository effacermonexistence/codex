# Backend self-repair — build124 (build123 + completable login)
Amendment (build124): the installed build123 proved that `claude auth login` is a code-paste flow even under a pseudo-terminal (`redirect_uri=platform.claude.com/oauth/code/callback`, "Paste code here if prompted"), so the headless child OS-1 spawned could never finish — the lever behind "클로드 연결시켜" (build122) and the build123 self-repair was hollow. Build124 opens the official flow in the owner's own Terminal window (`open -a Terminal ~/Library/Application Support/OS-1/auth-flows/claude-login.command`) and waits for `claude auth status` to turn logged-in (≤300 s); the code is pasted by the owner into that terminal and never passes through OS-1.
Goal: OS-1 must never end a request with "선택한 Claude 실행 환경이 없고 Codex 실행 환경도 없습니다" and wait for a human. A dead-backend preflight is a repair trigger: diagnose why each backend is unusable, run the repair OS-1 can do on its own (the official Claude login the owner approves in the browser), continue the same request once a backend is back, and otherwise hold the request and replay it automatically when health recovers. The fleet must stop advertising dead backends as capacity.
Source: owner instruction "자기가 셀프로 고칠 수 있게 만들어놔"; Fleet jobs fb4fcdb0/a2334685/97ff1a3f/a727c618/05f6607e all placed on the Air and failed at preflight with that exact message; `claude auth status --json` → `loggedIn:false`; Codex weekly bucket 100 % used, `resetsAt` 2026-09-19T20:51:15Z; `os1 fleet-snapshot` shows the Air advertising `has_claude=true has_codex=true` (Fleet.swift:419-420 checks only executable presence); the Pro's last heartbeat is 2 h stale (offline).
Allowed: BackendHealth model + cache (`~/Library/Application Support/OS-1/backend-health.json`), preflight self-repair in `runTask` (diagnosis, bounded official `claude auth login` through the existing lease/cooldown gate that the app already grants with `OS1_ALLOW_AUTHENTICATION=1`, catalog re-probe, continue in place), a `backend_unavailable` blocker with the diagnosis carried to the app, `os1 backend-health [--refresh]`, app-side recovery monitor that replays the preserved preflight-only request once per observed recovery, truthful fleet heartbeat capability flags, fixtures, release build, verified local install.
Forbidden: reading, storing or transferring credentials; performing the OAuth approval on the owner's behalf; opening login flows from the headless fleet agent (no `OS1_ALLOW_AUTHENTICATION` there); changing the fleet heartbeat contract (field set is signed by the gateway); replaying a dispatched or write-uncertain failure (only `preflightOnly` holds are replayed); paid model calls during self-tests.
Failure: (1) `executableProviderPreference` throws at preflight with no failure notice and no recovery step; the app stores `.unclassified/notDispatched` and shows "Needs attention". (2) The reconnect lever exists only behind the explicit "클로드 연결시켜" control. (3) Nothing watches for the backend to return, so the held request stays held forever. (4) Heartbeat `has_*` = executable present, so the gateway keeps placing jobs on a node that cannot run them.
Expected: preflight with no usable backend → activity "OS1이 작업 이어가는 중" with the per-backend diagnosis → if Claude is logged out and login is permitted, the official login opens and the run waits (≤300 s) → on success the same request proceeds without a new user turn; otherwise a `backend_unavailable` notice with the diagnosis and the earliest recovery time is preserved; the app polls health at most once a minute while such a hold exists and replays it once health reports a usable backend; the Air's heartbeat now advertises `has_claude=false has_codex=false` until either recovers.
Validation: `BackendHealthFixture` (classification, cache staleness, repair-plan order, hold message contents, JSON round trip), CLI self-test additions (health classification from catalog notes, advertised fleet capabilities), full fixture suite, CLI + app self-tests, release build, verified local install with queue preservation, live checks on the installed build: `os1 backend-health --refresh`, `os1 fleet-snapshot` capability flags, and a real preflight run that opens the official Claude login.
Stop: adopted local install + fixtures green + live heartbeat shows truthful flags, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via `~/.os1/recovery/session-index-claude-reconnect-build122-*` receipts; preserve the latest session file.

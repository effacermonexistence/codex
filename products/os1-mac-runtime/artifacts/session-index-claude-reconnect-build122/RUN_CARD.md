# Codex session-index lockout and Claude reconnect — build122
Goal: pressing CODEX in the rail must never pop "Codex session index could not be read" while Codex Desktop is merely writing its database; an expired Claude Code OAuth session must have an in-OS1 recovery lever ("클로드 연결시켜") instead of a dead end.
Source: user screenshot (dialog "Codex session index could not be read", CODEX pane empty, Claude turn "Failed to authenticate: OAuth session expired and could not be refreshed"); reproduction on the live index (`~/.codex/state_5.sqlite`, WAL, written continuously by Codex Desktop: plain read-only works now — the failure is a transient lock, OS1 set no busy timeout); `claude auth status --json` → `loggedIn:false` on the Air; OS-1 Fleet job `17b70b3c-…` (this exact request, routed to the Pro) ended `effectsUncertain`, its partial work pushed as `fix/claude-oauth-reconnect-20260914` (e3a433f).
Allowed: read-only Codex index access hardening (busy timeout, bounded retries, immutable-snapshot fallback), busy timeout on the transcript database, integrating the Pro's Claude connection target (probe `claude auth status --json`, bounded `claude auth login --claudeai` under the existing lease/cooldown), fixtures, release version and local verified installer.
Forbidden: writing to any Codex database, performing the OAuth login on the user's behalf, credential transfer between machines, held queue replay, external deployment.
Failure: (1) `codexSessions()` opened the WAL index with no `sqlite3_busy_timeout`; a Codex checkpoint or exclusive-mode writer at that instant made `sqlite3_prepare_v2` fail immediately → dialog. (2) When the Air's Claude OAuth refresh token was invalidated (rotated by the Pro sharing the account), every Claude dispatch failed `.authenticationRequired` with no recovery flow, while GitHub and R2 already had one.
Expected: `CodexSessionIndex` (OS1Context) reads with busy timeout 1.5s, retries transient locks, then answers from an `immutable=1` snapshot (may trail the WAL by moments — acceptable for a browse list); the dialog appears only with the underlying cause appended when even the snapshot fails; the transcript read waits out checkpoints; "클로드 연결시켜" probes and, when logged out, opens the bounded `claude auth login` browser flow (the user completes it — OS1 never handles the credentials).
Validation: `CodexSessionIndexFixture` (WAL fixture reproduces the reader lockout via exclusive locking mode, asserts the snapshot answer, checkpoint-before-lock reflects Codex's steady state), Pro's `TakeoverTests` additions (OAuth expiry classifies as `.authentication`, claude lease is its own service), full fixture suite, CLI and app self-tests, release build, verified local install with queue preservation.
Stop: adopted local install + fixtures green, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via the private installer receipts under `~/.os1/recovery/`; preserve the latest session file.
Residual (documented): the Air's Claude backend stays down until the user completes the re-login ("클로드 연결시켜" in OS-1, or `claude` → `/login` in a terminal); Codex models return at the 2026-09-19 20:51Z quota reset. The immutable snapshot can trail the newest Codex sessions by moments during a held lock.

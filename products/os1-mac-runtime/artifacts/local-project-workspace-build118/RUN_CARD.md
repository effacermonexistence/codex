# Local project workspace resolution — build118
Goal: "OS1 수정 가능하냐?" / "OS1 앱 … 고쳐" sent from an ordinary OS1 conversation (workspace HOME) must bind the OS-1 source checkout and, for a change request, dispatch the backend inside it — instead of a generic read-only answer that the source is "not bound".
Source: installed build117, task-events `cdfb4b0f-d2b7-48ca-8dc9-d266a1be0c80` (objective "야 여기서 OS1 수정 가능하냐?" → claude/fable/low read-only answer "OS-1 소스 리포지터리는 이 세션에 바인딩되어 있지 않습니다"), Claude session `cf28ef19…` (candidate list capped at 8 sorted roots, OS-1 checkout absent), `~/.codex/config.toml` project table (OS-1 checkout registered), sessions store (63 conversations on `/Users/lua`, 7 on `/Users/LUA`).
Allowed: preparation-intent rules, local-workspace project root resolution from the existing registry, candidate ordering handed to backends, self-test regressions, release version and local verified installer.
Forbidden: recursive HOME search, creating or registering directories, changing the user's Codex or Claude configuration, held queue replay, external deployment, unrelated UI or runtime rewrite.
Failure (three layers): (1) a feasibility question about a registered project was not a preparation intent, so no project was bound; (2) the `os1-clodex` adapter binds *the conversation's workspace* as the source, so from HOME it could only bind HOME; (3) the registry candidate list handed to Claude is bounded to 8 lexicographically sorted roots and the OS-1 checkout (`…/Codex/OS1-…`) sorted after `…/Codex/2026-…`, so Claude never saw it. Separately, a write request naming the project but using a verb outside the fixed change-verb list ("…한 줄 추가해") became a bare non-modifying preparation.
Expected: feasibility question + registered project + change verb → `prepare` without `modifies`; `LocalProjectWorkspace` resolves the project root (requested workspace when inside the tree, else the most recently changed registered root, fleet/temp roots excluded) and the run, baseline and later turns use it; a project-naming write-scope request is modifying even without a listed verb; OS-1 requests prioritise OS-1 source roots in the candidate list.
Validation: self-test cases (intent, root resolution, modifying vs bare preparation), debug replay of the question from HOME (control answer binding the checkout, no model), debug replay of a one-line change request from HOME (Claude `sonnet/medium` edited exactly that file inside the checkout; the file was restored afterwards), full release build, verified local install with queue preservation, replay on the installed build.
Stop: adopted local install + both replays behave as expected, or a genuine unsafe active-state blocker.
Rollback: previous local app and CLI via the private installer receipts under `~/.os1/recovery/`; preserve the latest session file.
Observed hazard (not changed here): a Claude backend working inside `effacermonexistence/*` follows the user's global remote-completion contract and committed the proof line on the current branch, then reported a GitHub push that had not happened; the local commit was dropped and the remote was unchanged. OS1 verifies the native record, not remote claims.

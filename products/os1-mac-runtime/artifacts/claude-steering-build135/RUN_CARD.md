# Claude live steering + queue that actually drains — build135
Goal: "스티어링은 안되고 큐잉만 된다, 끼어들기 버튼이 비활성이다" and "작업이 끝나도 쌓인 큐가 저절로 안 나간다"를 코드로 끝낸다.
Source: owner report + live state: `canSteer` was hard-gated `active.provider == .codex` while every run is Claude (Codex quota until 9/19), so the steer button could never enable and typed corrections always queued; `pausedQueueIDs = Set(queued)` on every app start froze the whole saved queue at each of today's five self-update relaunches; two sessions (3E2DC120, FDC2AC9C) hold queued work behind pre-build134 `effectsUncertain` failures whose auto-readback ran once before the verdict contract existed, so nothing ever re-examines them.
Allowed: Claude runs under an owning OS-1 submission switch to `--input-format stream-json` (prompt + live corrections as stdin user messages; positional prompt otherwise unchanged); `ClaudeSteerDriver` (mailbox lease open → .sending/.accepted on write → .persisted once the turn carrying the correction completes → close stdin when current turn done and nothing pending; corrections lost to the closing race stay unconsumed for the existing continuation path); `ExecutionStream.resultCount/turnOpen`; corrected-objective fold for validation (Codex parity); app gates accept claude; `releaseRestartHolds` (60 s after launch, clean sessions only — the installer's 3 s post-relaunch assertions stay safe); `resumeStaleReconciliations` (one readback per verdict-contract generation via `verdictReconciled`, then OS1_EFFECTS decides); `commandOutput(interactiveStdin:)`.
Forbidden: steering source-only answers; injecting anything but the owner's mailbox text; closing receipts as persisted before the correction's turn completed; releasing restart holds for sessions with failure/preparation holds or a user pause; more than one retroactive readback per failure.
Validation: 9/9 suites (CLI 58 checks incl. steer-argument assembly and stream turn counting; parallel/steering/queue suites green); **live CLI proof**: run started, correction injected mid-flight, receipt `persisted turn=result-1`, `persistedCorrectionIDs=1`, final output followed the correction (English counting for a Korean-count prompt); staged and self-installed as build135; post-install watch: stale sessions get their verdict readbacks and the held queue drains.
Stop: build135 self-installed + the live queue moving again.
Rollback: `~/.os1/recovery/self-update-build134-*`.

# Attached images reach the model; held failures re-examined per build — build137
Goal: the owner's queued request "이미지를 첨부하면 코덱스처럼 이미지가 딱 나와야지 이미지 주소가 왜 나와" and the failed task in front of it, done by OS-1's maintainer instead of waiting on a button.
Source: owner "그럼 내가 왜 해야 되는데 니가 해". Evidence: both screenshot files behind the held queue exist (`/Users/LUA/Desktop/Screenshot 2026-09-15 at 11.47.16 AM.png`, `…11.48.19 AM.png`); the backend had declared "스크린샷 원본은 존재하지 않습니다" because the read-only lane (dontAsk, HOME workspace, safe-mode) cannot open Desktop — a path string is useless to it. The readback/resume budgets for that failure were spent under build134–136.
Allowed: `PromptAttachments` (parse the app's quoted-path block), `ImageInput` (ImageIO thumbnail ≤1600 px, JPEG 0.85, ≤4.5 MB), Claude stream-json user message with base64 image blocks (driver now also runs without a mailbox when images are present), Codex `localImage` turn items, inline NSTextAttachment previews in the transcript (path block hidden in display, kept in storage/copy), `reconciledUnderBuild` so a held failure gets one fresh readback after every self-install, PR #101/#102 merged on the owner's instruction, fixtures/self-tests.
Forbidden: reading anything but the referenced image bytes; sending images for source-only answers; unbounded image sizes; more than one readback per (failure, build).
Validation: attachments fixture 7; CLI self-test 59 (3000×2000 PNG → ≤1600-edge JPEG under the byte cap, unreadable path skipped); all nine suites; **live proof** — a run with the real screenshot attached answered by describing the image content (the OS-1 rail's pink pill and grey icon), i.e. the model received pixels, not a path; staged and self-installed as build137; post-install, the held screenshot session is re-examined under build 137 and its queue drains.
Stop: build137 self-installed and the held queue moving.
Rollback: `~/.os1/recovery/self-update-build136-*`.
